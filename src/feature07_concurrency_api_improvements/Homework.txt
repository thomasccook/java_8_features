Assignment:
	Using ConcurrentHashMap
		- Create a list of prefixes
		- Create a list of base words
		- create a list of suffixes
		- https://ies.ed.gov/ncee/edlabs/regions/southeast/foundations/resources/firstgrade/rec3/prefix_base_word_suffix.pdf
		- Create a list of word combinations that aren't real (e.g - unpersonful)
		- Have multiple threads work ConcurrentHashMap simultaneously
			- One adds base
			- One adds prefix + base
			- One adds prefix + base + suffix
			- One add base + suffix
			- One marks words that aren't real
